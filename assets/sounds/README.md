# Sound Effects

The game uses local MP3 files in this folder for gasing spin, metal hit, whoosh, power-up, ultimate impact effects, and background music.

`client/src/soundManager.js` loads these files with the WebAudio API and falls back to generated sounds if an asset fails to load.

See `ATTRIBUTION.md` for source and license notes.
